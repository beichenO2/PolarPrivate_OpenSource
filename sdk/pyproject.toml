[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "privportal-sdk"
version = "0.7.0"
description = "Lightweight SDK for PolarPrivate — LLM proxy calls and cross-service identity bindings"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.24",
]

[tool.hatch.build.targets.wheel]
packages = ["src/privportal_sdk"]
